/**
 * Application entry point
 * IMPORTANT: unistyles must be imported FIRST
 */
import './unistyles';

import { registerRootComponent } from 'expo';
import { App } from './App';

registerRootComponent(App);

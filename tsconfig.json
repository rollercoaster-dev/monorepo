{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "composite": true
  },
  "include": [],
  "references": [
    // Packages
    { "path": "./packages/rd-logger" },
    { "path": "./packages/openbadges-types" },
    { "path": "./packages/openbadges-core" },
    // Apps
    { "path": "./apps/openbadges-modular-server" }
    // Note: shared-config, openbadges-ui, and openbadges-system excluded
    // - shared-config: config-only package with no TypeScript source files
    // - openbadges-ui, openbadges-system: Vue SFC declaration limitations prevent composite mode
  ]
}

#!/bin/sh
set -e

# Fail fast if the install is stale after pulling new deps or workspace links.
bun run check:install

# Run lint-staged (eslint + prettier on staged files)
bunx lint-staged

# Run type-check on the entire project
echo "Running type-check..."
bun run type-check

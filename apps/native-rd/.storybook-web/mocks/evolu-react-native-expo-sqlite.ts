/**
 * Storybook ESM mock for @evolu/react-native/expo-sqlite
 *
 * Provides a stub deps object so createEvolu() can be called
 * without a real SQLite backend.
 */
export const evoluReactNativeDeps = {};

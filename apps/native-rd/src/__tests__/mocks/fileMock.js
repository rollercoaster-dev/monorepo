/**
 * Mock for binary asset imports (png, jpg, jpeg, gif, webp, svg)
 *
 * Returns a plain string stub so that `import img from './photo.png'`
 * resolves without requiring a real asset bundler.
 */
module.exports = "test-file-stub";

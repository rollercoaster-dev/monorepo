/**
 * Stub for RN 0.81+ internal ESM spec files (specs_DEPRECATED).
 * These files use `export var` syntax which fails in Jest's
 * requireActual via the RN mock setup.
 */
module.exports = {
  __INTERNAL_VIEW_CONFIG: {},
  default: {},
};

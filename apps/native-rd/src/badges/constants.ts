/**
 * Shared badge constants that aren't theme-driven.
 *
 * BADGE_CANVAS_BACKGROUND is the background colour of the badge "paper" —
 * the surface the badge artwork sits on in the designer preview, detail
 * screen, and stories. It's intentionally fixed (not theme-derived) so the
 * badge artwork looks consistent across light/dark/accessibility variants
 * and matches the baked PNG that gets shared off-device.
 */

export const BADGE_CANVAS_BACKGROUND = "#ffffff";

import { StyleSheet } from "react-native-unistyles";

export const styles = StyleSheet.create(() => ({
  container: {
    position: "relative",
  },
  icon: {
    position: "absolute",
    left: 0,
    top: 0,
  },
}));

# Rollercoaster.dev Monorepo Environment Configuration
# Copy this file to .env and customize values for your local environment

# ==============================================================================
# GENERAL ENVIRONMENT
# ==============================================================================

# Node Environment (development, production, test)
# Affects build optimizations, logging behavior, and default configurations
NODE_ENV=development

# ==============================================================================
# CENTRALIZED APP PORTS
# ==============================================================================
# These ports are passed via Turbo's globalEnv when running `bun dev` from root.
# NOTE: When running apps directly (e.g., via tmux/start-rollercoaster), each
# app needs PORT set in its own .env file since Turbo isn't involved.

# openbadges-modular-server (OB 2.0/3.0 API)
MODULAR_SERVER_PORT=3666

# openbadges-system backend (Bun/Hono)
SYSTEM_SERVER_PORT=8888

# openbadges-system frontend (Vite dev server)
SYSTEM_VITE_PORT=7777

# ==============================================================================
# LOGGING (for @rollercoaster-dev/rd-logger)
# ==============================================================================

# Log Level (debug, info, warn, error, fatal)
# Controls which log messages are output based on severity
LOG_LEVEL=info

# Debug Queries (true, false)
# When true, logs ALL database queries at debug level
# When false, only logs slow queries (above threshold)
DEBUG_QUERIES=false

# ==============================================================================
# CI/CD ENVIRONMENT
# ==============================================================================

# CI Flag (true when running in CI)
# Automatically set by most CI providers (GitHub Actions, etc.)
# CI=true

# ==============================================================================
# GITHUB CLI (gh)
# ==============================================================================

# GitHub Personal Access Token for gh CLI authentication
# Required for: fetching issues, creating PRs, repo operations
# Create at: https://github.com/settings/tokens
# Scopes needed: repo, read:org (for org repos)
# For Claude Code Web: Set this in the Web UI environment variables
# GH_TOKEN=ghp_your_token_here

# ==============================================================================
# FUTURE: APPLICATION-SPECIFIC VARIABLES
# ==============================================================================
# The following sections are placeholders for when apps are migrated
# to the monorepo (Phase 3 of migration plan)

# --- Open Badges System (apps/openbadges-system) ---
# DATABASE_URL=postgresql://localhost:5432/openbadges
# JWT_SECRET=your-secret-key-here
# API_BASE_URL=http://localhost:3000

# --- Open Badges Modular Server (apps/openbadges-modular-server) ---
# SERVER_PORT=8080
# BADGES_STORAGE_PATH=/var/lib/badges
# ENABLE_OB_3_0=true

# --- Landing Page (apps/landing) ---
# VITE_API_URL=http://localhost:3000
# VITE_ENABLE_ANALYTICS=false

# ==============================================================================
# PACKAGE-SPECIFIC OVERRIDES
# ==============================================================================
# Individual packages may have their own .env.example files
# Check packages/[package-name]/.env.example for package-specific variables

{
  "name": "claude-workflows",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Executable helpers for Claude Code workflow commands and agents",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "workflow": "src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix"
  },
  "dependencies": {
    "claude-knowledge": "workspace:*"
  },
  "devDependencies": {
    "@rollercoaster-dev/shared-config": "workspace:*"
  }
}

# Plan: Visual Verification Wrapper

## What exists

Three new files already written on `feat/visual-verification-wrapper`:

1. `.claude/commands/visual-auto-issue.md` — wrapper command for `/auto-issue`
2. `.claude/commands/visual-work-on-issue.md` — wrapper command for `/work-on-issue`
3. `scripts/generate-sitemap.ts` — sitemap generator for route discovery

## Steps

1. **Commit the three new files** with a conventional commit message
2. **Push the branch** to origin
3. **Create a PR** against main with a description of the feature

No code changes needed — the files are ready as-is.
